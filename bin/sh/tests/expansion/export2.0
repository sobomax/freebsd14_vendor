# $FreeBSD: 57f64e7f7f7be997df05f8af8aa8b43b75fa06a0 $

w='@ @'
check() {
	[ "$v" = "$w" ] || echo "Expected $w got $v"
}

export v=$w
check

HOME=/known/value
check() {
	[ "$v" = ~ ] || echo "Expected $HOME got $v"
}

export v=~
check

check() {
	[ "$v" = "x:$HOME" ] || echo "Expected x:$HOME got $v"
}

export v=x:~
check
