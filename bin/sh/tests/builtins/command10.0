# $FreeBSD: 2c1adf1eb0bd429b69c3aaa7607cf24623c5ed05 $

failures=0

check() {
	if ! eval "[ $* ]"; then
		echo "Failed: $*"
		: $((failures += 1))
	fi
}

check '"$(f() { shift x; }; { command eval f 2>/dev/null; } >/dev/null; echo hi)" = hi'

exit $((failures > 0))
