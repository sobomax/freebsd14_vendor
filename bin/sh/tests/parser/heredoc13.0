# $FreeBSD: 225d4f08f4929f4263fc373b4ead33a5c5f5fcc2 $

failures=0

check() {
	if ! eval "[ $* ]"; then
		echo "Failed: $*"
		: $((failures += 1))
	fi
}

check '"$(cat <<""

echo yes)" = "yes"'

check '"$(cat <<""
yes

)" = "yes"'

exit $((failures != 0))
